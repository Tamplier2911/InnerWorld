const spinnerStyles = (theme) => ({
  container: {
    height: '85vh',
    display: 'flex',
    padding: '8px 0px',
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default spinnerStyles;
